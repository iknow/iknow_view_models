---
version: "{build}"
image: ubuntu1804

stack:
  - ruby 2.5.3, postgresql

build: off

install:
  - sudo -u postgres createdb -O appveyor iknow_view_models
  - gem install bundler
  - bundle install

before_test:
  - ruby -v
  - bundle -v

test_script:
  - bundle exec rake
